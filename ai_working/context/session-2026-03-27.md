# Session Summary — 2026-03-27

## What was accomplished

### 1. Company Lookup Feature — Tests + ADR-005 (partial)
- Phase 4a (backend tests for company search/classify) was **in progress** when session pivoted
- Phase 4b (CompanyLookup component tests) still pending
- Phase 4c (ADR-005 + docs sync) still pending
- The feature itself is fully built and working (Phases 1-3 complete from previous session)

### 2. AU Industry Classification Research
- Deep investigation into ANZSIC 2006 limitations (19 divisions too coarse, no revision since 2006)
- ABS ran public consultation in 2022 on ISIC Rev.5 adoption — no decision date yet
- Identified two priority enrichment datasets:
  - **ABS Business Counts** at sub-division level (96 ANZSIC codes) — Priority 1
  - **ABN Lookup API** for per-company ANZSIC class — Priority 2
- Documented as DataScout backlog items

### 3. ADR-006: DataScout Acquisition Pattern
- Formalised the data ingestion pipeline pattern that emerged across 8+ scripts
- Seven-stage pluggable pipeline: DISCOVER → ACQUIRE → PARSE → NORMALISE → CONCORDANCE → LOAD → VERIFY
- Scout vs Harvest distinction
- Script naming convention
- Full inventory of 16 current scripts mapped to pipeline stages
- DataScout backlog with 5 prioritised sources
- Commit: `e831e8d`

### 4. Rob Pike's 5 Rules Applied to LLM Development
- Discussion mapping Pike's rules to actual platform development experience
- Rule 5 ("Data dominates") validated — 535K rows, trivial algorithms, hard data decisions
- Proposed Rule 6: "Context dominates" — CLAUDE.md, ADRs, session logs drive LLM effectiveness

### 5. ADR-007: Performance Instrumentation Strategy
- Three-layer observability stack designed:
  - Layer 1: API timing middleware → `api_request_log` table
  - Layer 2: `pg_stat_statements` extension for query profiling
  - Layer 3: Performance baseline test suite with threshold regression detection
- Six decisions covering stack selection, schema, data storage, deferrals
- CLAUDE.md updated with cross-cutting observability line
- Commit: `e185932`

## Commits this session (2)

| Hash | Message |
|------|---------|
| `e831e8d` | docs: ADR-006 DataScout acquisition pattern |
| `e185932` | docs: ADR-007 Performance instrumentation strategy |

## Outstanding work (carry forward)

### Immediate (next session)
1. **Company lookup tests** — Phase 4a/4b/4c still pending:
   - Backend tests for `TestCompanySearch` and `TestCompanyClassify` in test_api.py
   - Component tests for `CompanyLookup.test.tsx`
   - ADR-005 (Company-to-Industry Mapping) — plan exists in `.claude/plans/kind-humming-biscuit.md`
   - Docs sync for company lookup feature

2. **Performance instrumentation build** (ADR-007 implementation):
   - Layer 1: Alembic migration for `api_request_log`, FastAPI middleware (`app/middleware/timing.py`)
   - Layer 2: Enable `pg_stat_statements`, admin endpoints (`GET /admin/slow-queries`, `GET /admin/request-stats`)
   - Layer 3: `tests/test_performance.py` baseline suite with thresholds for all 19+ endpoints

### DataScout backlog (future sessions)
- ABS Business Counts at sub-division level (96 ANZSIC codes)
- ABN Lookup API (per-company ANZSIC class)
- ISIC Rev.5 concordance tables
- ABS TableBuilder microdata
- Stats NZ employment data

## Environment notes
- **Two Python installations**: Miniconda (Claude Code) vs Microsoft Store Python 3.13 (PowerShell). Packages installed in one aren't available in the other.
- **Windows uvicorn --reload**: Doesn't detect new files added to existing packages. Must restart server after adding new endpoint modules.
- **Anthropic API key**: Must be in `src/backend/.env` (not project root `.env`). Loaded via Pydantic Settings.
- **Backend**: `cd src/backend && python -m uvicorn app.main:app --reload --port 8000`
- **Frontend**: `cd src/frontend && npx vite --host`

## Current state

| Metric | Value |
|--------|-------|
| API endpoints | 21 (19 + 2 company) |
| Dashboard pages | 6 |
| Total tests | ~214 (some company tests pending) |
| ADRs | 7 (001-007) |
| Data rows | ~537,633 |
| Latest commit | `e185932` on `master` |
